name: baseballinfoapp
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  provider: ^4.1.3  # Flutter Provider # 参考 KBOY Youtube https://www.youtube.com/watch?v=iN2IjSQR7Fs
  firebase_core: ^0.4.5  # add dependency for Firebase Core
  cloud_firestore: ^0.13.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


